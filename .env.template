# === OpenAI Config ===
# Coloca aquí tu API key de OpenAI
OPENAI_API_KEY=your_openai_api_key_here


# Modelo a utilizar (gpt-3.5-turbo recomendado en desarrollo, gpt-4-turbo en producción)
OPENAI_MODEL=gpt-3.5-turbo


# === Postgres Config ===
POSTGRES_USER=kopi
POSTGRES_PASSWORD=kopi_password
POSTGRES_DB=kopi_chat
POSTGRES_PORT=5432

# Host depende del entorno:
# - Local: usa localhost
# - En Docker: usa db
POSTGRES_HOST=localhost
# POSTGRES_HOST=db


# === Database URL ===
# Ejemplo local:
# DATABASE_URL=postgresql+asyncpg://kopi:kopi_password@localhost:5432/kopi_chat
#
# Ejemplo contenedor (Docker Compose):
# DATABASE_URL=postgresql+asyncpg://kopi:kopi_password@db:5432/kopi_chat
#
# Ejemplo Render (o DB remota con psycopg):
# DATABASE_URL=postgresql+psycopg://USER:PASSWORD@HOST:PORT/DBNAME

DATABASE_URL=
